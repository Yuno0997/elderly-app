PORT=4000
JWT_SECRET=replace-with-a-long-random-secret
APP_ENV=production
ALLOW_DEMO_SEED=false
FIRST_ADMIN_BOOTSTRAP_KEY=replace-with-one-time-bootstrap-key
# Optional override (default is server/data/backups)
# BACKUP_DIR=server/data/backups
